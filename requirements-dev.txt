# Test-only dependencies. Kept out of requirements.txt so the Render build stays lean —
# it installs requirements.txt only.
#
#   python -m venv .venv
#   .venv/Scripts/python -m pip install -r requirements.txt -r requirements-dev.txt   # Windows
#   .venv/bin/python     -m pip install -r requirements.txt -r requirements-dev.txt   # macOS/Linux
#   .venv/Scripts/python -m pytest
#
# Pinned for the same reason as requirements.txt: a test suite that passes against a different
# dependency set than production isn't telling you much.

pytest==9.1.1
httpx==0.28.1          # required by fastapi.testclient.TestClient
ruff==0.16.8           # lint + format check; config in ruff.toml
